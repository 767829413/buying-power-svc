allOf:
  - $ref: '#/components/schemas/TokenKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - token
        properties:
          token:
            type: string
            description: jwt token